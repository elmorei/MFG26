{
  "name": "mfg26-strategy-poc",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "doctor": "node ./scripts/doctor.mjs",
    "setup:clean": "npm cache verify && npm install"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  },
  "packageManager": "npm@10.8.2",
  "engines": {
    "node": ">=20.18.0",
    "npm": ">=10.0.0"
  }
}
